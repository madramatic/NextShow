import React from 'react';
import { View, Text } from 'react-native';

export default function SeatsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Seat Map (UI only)</Text>
    </View>
  );
}
